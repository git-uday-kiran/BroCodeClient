<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BroCodeClient" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.BroCodeClient.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.BroCodeClient.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.BroCodeClient.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.BroCodeClient.AppWidgetContainerParent" parent="@android:style/Theme.DeviceDefault">
        <!-- Radius of the outer bound of widgets to make the rounded corners -->
<!--        <item name="appWidgetRadius">16dp</item>-->
        <!--
        Radius of the inner view's bound of widgets to make the rounded corners.
        It needs to be 8dp or less than the value of appWidgetRadius
        -->
<!--        <item name="appWidgetInnerRadius">8dp</item>-->
    </style>

    <style name="Theme.BroCodeClient.AppWidgetContainer" parent="Theme.BroCodeClient.AppWidgetContainerParent">
        <!-- Apply padding to avoid the content of the widget colliding with the rounded corners -->
<!--        <item name="appWidgetPadding">16dp</item>-->
    </style>
</resources>